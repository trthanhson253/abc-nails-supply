import React from "react";

// loading nhỏ
const Loader = () => {
  return (
    <span
      style={{
        width: "100px",
        height: "100px",
        margin: "auto",
        display: "block",
      }}
    >
      <div class="loader"></div>
    </span>
  );
};

export default Loader;
